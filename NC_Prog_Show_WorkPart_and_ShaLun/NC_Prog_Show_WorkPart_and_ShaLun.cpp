// NC_Prog_Show_WorkPart_and_ShaLun.cpp : 定义 DLL 的导出函数。

/*
*	由于该模板预编译头必须引用windows.h，而其中定义了WIN32
*   这会影响库设置中的预处理代码
*   若要使用预编译头，则pch.h必须包含
*	因此采取定义PCH_H的方法使得在pch.h中跳过包含项
*   
*   上述问题应该还可以通过单独设置cpp文件的预编译头解决。
*/

#include "pch.h"
#define NC_Prog_Show_WorkPart_and_ShaLun_EXPORTS
#include "NC_Prog_Show_WorkPart_and_ShaLun.h"
